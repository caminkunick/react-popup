{
  "name": "@caminkunick/react-popup",
  "version": "1.0.0",
  "description": "",
  "main": "index.tsx",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "push": "ssh-add -D && ssh-add ~/.ssh/id_ed25519_cmk && git push origin main && ssh-add -D"
  },
  "author": "caminkunick",
  "license": "ISC",
  "peerDependencies": {
    "@mui/material": "^5.8.0",
    "@fortawesome/react-fontawesome": "^0.1.18",
    "@fortawesome/pro-duotone-svg-icons": "^6.1.1",
    "@fortawesome/fontawesome-svg-core": "^6.1.1"
  }
}
